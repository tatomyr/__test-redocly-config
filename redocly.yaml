# apiDefinitions:
#   alias: resources/petstore-with-errors.yaml

apis: 
  x-andrew@v1:
    root: resources/pets.yaml
    lint: 
      extends: []
      rules: 
        operation-summary: warn
    features.openapi: 
      disableSidebar: true
    
  andrew: 
    root: resources/petstore-with-errors.yaml
    lint: 
      # extends: []
        # - recommended
      rules: 
        no-empty-servers: error

  rebilly@test:
    root: resources/rebilly.yaml
    lint:
      rules:
        no-unused-components: error
      extends:
        - recommended

  x-andrew@next: 
    root: resources/pets.yaml
    features.openapi:
      disableSidebar: true
    lint:
      rules:
        operation-summary: warn
        operation-2xx-response: off
        
  x-andrew@nested: 
    root: resources/pets.yaml
    lint:
      extends:
      - resources/config1.yaml
      - resources/config2.yaml
      - recommended
      rules:
        operation-summary: warn
#         operation-2xx-response: off



organization: redocly-config

lint: 
  # extends: []
  rules: 
    operation-summary: error
#     operation-4xx-response: warn

referenceDocs: 
  hideLogo: true
